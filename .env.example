# Numera — environment variables
#
# Version 1 runs entirely client-side with local persistence, so no environment
# variables are required to run the app locally. This file exists as a placeholder
# for future online-multiplayer configuration (plan §3, §27):
#
# DATABASE_URL=
# REDIS_URL=
# NEXT_PUBLIC_SENTRY_DSN=
